<div id="<%= dom_id package %>">
  <p>
    <strong>Name:</strong>
    <%= package.name %>
  </p>

  <p>
    <strong>Price:</strong>
    <%= package.price %>
  </p>

  <p>
    <strong>Plan:</strong>
    <%= package.plan.name %>
  </p>
  
</div>

<h1>Pacote: <%= @package.name %></h1>

<p><strong>Plano:</strong> <%= @package.plan.name %></p>
<p><strong>Valor:</strong> R$ <%= @package.price %></p>

<h3>Serviços Adicionais:</h3>
<ul>
  <% @package.additional_services.each do |s| %>
    <li><%= s.name %> - R$ <%= s.price %></li>
  <% end %>
</ul>
